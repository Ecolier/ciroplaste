{
  "name": "crp",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "packageManager": "yarn@4.5.0",
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
