{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["build", "dist"]
    },
    "dev": {
      "env": ["DATABASE_URI", "PAYLOAD_SECRET_FILE"],
      "cache": false,
      "persistent": true
    }
  }
}
